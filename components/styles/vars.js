import { Dimensions } from 'react-native';

const { width, height } = Dimensions.get('window');

export default {
    white: '#fff',
    red: 'red',
    orange: 'orange',
    green: 'green',
    blue: 'blue',
    lightBlue: 'lightblue',
    width,
    height,
};