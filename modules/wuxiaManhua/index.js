import { moduleName, scrollFilter, isNovel } from './config/consts';
import { mangaUrl, mangaDirectoryUrl, scrollingPath } from './config/Network';
import actions from './actions';

export default { moduleName, actions, mangaUrl, searchPath : scrollingPath, mangaDirectoryUrl, scrollFilter, isNovel };