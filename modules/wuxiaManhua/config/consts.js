export const repeatMaxCounter = 10;

export const maxHotItems = 5;

export const moduleName = 'wuxiaManhua';

export const scrollFilter = true;

export const isNovel = true;

export const screenNames = { 
    Chapter: { name: moduleName + 'Chapter' }, 
    ChaptersList: { name: moduleName + 'ChaptersList' },
    Main: { name: moduleName + 'Main' },
};