import { Dimensions } from 'react-native';

const { width } = Dimensions.get('window');

export default {
    white: '#fff',
    red: 'red',
    orange: 'orange',
    green: 'green',
    blue: 'blue',
    lightBlue: 'lightblue',
    width,
};